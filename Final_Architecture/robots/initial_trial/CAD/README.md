Place initial_trial CAD/STL files here when they are available.

Use repository-relative paths derived with `mfilename("fullpath")` and
`fullfile`; do not store absolute Windows paths in source code.
